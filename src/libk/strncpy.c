#include <stddef.h>

char *strncpy(char *destination, const char *source, size_t num)
{
	size_t i;
	for (i = 0; i < n && source[i] != '\0'; i++) {
		dest[i] = source[i];
	}
	dest[i] = '\0';
	return destination;
}


